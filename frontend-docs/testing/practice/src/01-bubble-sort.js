/**
 * 練習 01：Bubble Sort
 *
 * 待實作：把陣列由小到大排序。
 * 加分：加入 early-exit 優化（某輪沒交換就停止）。
 *
 * 提示：
 * - 兩兩比較相鄰元素，大的往後換
 * - 每輪結束後，最大值會「浮」到最後
 * - 沒交換就代表已經排好，可以提早 break
 * - 注意：不要修改原始陣列（看最後一個測試）
 *
 * 完成後跑：npm test 01
 */
export function bubbleSort(arr) {
  // TODO: 在這裡實作 bubble sort
  // 暫時直接回傳原陣列，所以大部分測試會失敗 🔴
  return arr
}
