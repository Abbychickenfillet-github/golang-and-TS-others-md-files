# 工作日誌 2026-01-02

## 待辦事項

- [ ] 練習 `git switch` vs `git checkout` 差異
  - 切換分支：`git switch main` vs `git checkout main`
  - 建立新分支：`git switch -c new-branch` vs `git checkout -b new-branch`
  - 還原檔案：`git restore file.txt` vs `git checkout -- file.txt`

- [ ] 複習 `git-line-endings-notes.md`
  - autocrlf 設定（Windows: true, Linux/Mac: input）
  - 檢出 = clone, pull, checkout, switch, restore
  - CRLF vs LF 轉換機制

## 今日完成

- [x] 設定 `git config core.autocrlf true`（Windows）
- [x] 修正 `git-line-endings-notes.md` 文件錯誤
- [x] 新增 switch vs checkout 說明
- [x] 完成多個功能 commit 並 push
