---
title: "C槽空間週檢紀錄"
---

# C槽空間週檢紀錄

> 每週由排程任務自動檢查一次，記錄可用空間趨勢，方便回顧是否需要清理。

## 記錄

2026-07-19 | 可用約 68 GB(掛載點代理值，非直接讀取 C 槽) | ✅ 空間充足
2026-07-26 | 可用約 55 GB(掛載點代理值，非直接讀取 C 槽) | ✅ 空間充足
2026-08-02 | 可用約 17 GB(掛載點代理值，使用率 97%) | ⚠️ 該清理了
2026-08-09 | 可用約 8.2 GB(掛載點代理值，使用率 99%) | 🔴 危險，請立即清理
2026-08-16 | 可用約 32 GB(掛載點代理值，共 476G，已用 445G，使用率 94%) | ✅ 剛好過關，但貼著門檻

## 備註

- 本 session 只能讀到已連接資料夾(Abby-notes)所在磁碟的掛載資訊，無法直接讀取 Windows C 槽本身的 `df` 數據，此處用掛載點所在磁碟作為代理參考(最新一次：共 476G，已用 445G，可用 32G，使用率 94%)。
- 趨勢：7/19 可用 68G → 7/26 可用 55G → 8/2 可用 17G → 8/9 可用 8.2G → 8/16 可用 32G。上週的危險區已解除，中間應該有做過一次清理(釋出約 24GB)，但使用率仍有 94%，只要再跑幾次 docker build 就會掉回警戒區。
- 若要精確得知 C 槽實際可用空間，請在 CMD 執行：

```
fsutil volume diskfree C:
```

- 判斷標準：可用 < 30GB → ⚠️ 該清理了；可用 >= 30GB → ✅ 空間充足。
