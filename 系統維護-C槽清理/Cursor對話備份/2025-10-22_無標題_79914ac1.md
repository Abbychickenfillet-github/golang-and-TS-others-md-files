# 無標題

> Cursor · 2025-10-22 21:40 · `79914ac1-f9a0-4f48-a2c6-548ea2b03a61` · 4 則

## 你
??

## Cursor
I can see you're getting a warning about Babel configuration not being supported with Turbopack in Next.js. The error message indicates that you have a `babel.config.js` file that's causing issues when running Next.js with Turbopack.

Let me check your current Babel configuration and help you resolve this:

## Cursor
I can see the issue! You have a `babel.config.js` file that's being used for Jest testing (as seen in the Jest configuration), but Next.js with Turbopack doesn't support Babel configuration. 

Here are your options to resolve this:

## Option 1: Remove Turbopack (Recommended for now)
Since you're using Babel for Jest testing, the simplest solution is to remove the `--turbopack` flag from your dev script:

## Cursor
## Option 2: Keep Turbopack but rename Babel config (Alternative)
If you want to keep using Turbopack, you could rename the `babel.config.js` to something like `babel.config.test.js` and update your Jest configuration to use it specifically for testing. However, this is more complex and Option 1 is simpler.

## Option 3: Use Next.js built-in Jest support (Future consideration)
Next.js has built-in Jest support that doesn't require Babel configuration, but this would require updating your Jest setup.

**I recommend going with Option 1** since it's the simplest solution and your app will work perfectly without Turbopack. Turbopack is still experimental and the performance difference might not be significant for your current development needs.

Now you can run `npm run dev` and it should work without the Babel warning!
