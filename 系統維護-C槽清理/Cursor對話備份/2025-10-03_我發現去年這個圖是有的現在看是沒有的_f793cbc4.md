# 我發現去年這個圖是有的現在看是沒有的

> Cursor · 2025-10-03 10:06 · `f793cbc4-3ef7-4001-bafc-47d9275b10b6` · 12 則

## 你
我發現去年這個圖是有的現在看是沒有的

## Cursor
我需要先了解一下您提到的問題。從您提供的截圖來看，這似乎是一個電競活動網站的頁面，顯示的是已結束的活動列表。讓我先檢查一下當前的代碼狀態，看看可能出現了什麼問題。

## Cursor
現在我需要檢查活動頁面的具體實作和圖片路徑問題。讓我先查看活動頁面的主要檔案：

## Cursor
現在讓我檢查後端的活動API以及圖片相關的路由設定：

## Cursor
現在讓我檢查活動的路由檔案：

## Cursor
現在我需要檢查資料庫中活動圖片的儲存方式，以及圖片路徑的處理：

## Cursor
現在我了解問題所在了！從資料庫資料可以看到，活動的圖片路徑都是直接用完整的 URL（如 `https://d1k8pxxip4mxx2.cloudfront.net/pub/media/...`），但在前端的 `EventCard.js` 中，它直接用 `picture || '/images/event-default.png'` 來顯示圖片。

問題是：
1. 資料庫中的 `event_picture` 欄位儲存的是完整的外部 URL
2. 但如果外部 URL 失效或無法訪問（比如 CDN 問題、網路問題等），圖片就會載入失敗
3. 雖然有 `onError` 處理會顯示預設圖片，但在您的截圖中顯示的是 "已結束" 狀態，說明這些活動的圖片可能確實已經無法正常載入

## Cursor
讓我檢查一下是否有預設的活動圖片檔案：

## Cursor
我發現問題了！在 `frontend/public/images/` 目錄中**沒有** `event-default.png` 檔案，但是 `EventCard.js` 中的錯誤處理會嘗試載入 `/images/event-default.png`。

現在我需要檢查幾個可能的解決方案：

## Cursor
現在我來協助您解決這個問題。我已經找到問題的根本原因：

**問題診斷：**
1. 您的資料庫中活動圖片儲存的是外部 CDN URL（如 `https://d1k8pxxip4mxx2.cloudfront.net/...`）
2. 這些外部圖片可能已經失效或無法訪問（可能是 CDN 過期、網站改版等原因）
3. 當圖片載入失敗時，系統會嘗試顯示預設圖片 `/images/event-default.png`
4. 但是這個預設圖片檔案不存在，導致顯示空白

**解決方案：**

## Cursor
讓我創建一個測試用的預設圖片檔案，然後改善圖片載入處理：

## Cursor
## 問題解決方案

根據我的分析，您的問題是因為：

1. **外部圖片失效**：資料庫中的活動圖片是外部 CDN URL，這些 URL 可能已經失效
2. **缺少預設圖片**：當外部圖片載入失敗時，系統嘗試顯示預設圖片 `/images/event-default.png`，但這個檔案不存在

### 解決方案

讓我為您提供一套完整的解決方案：

**方案 1：創建預設圖片檔案**
