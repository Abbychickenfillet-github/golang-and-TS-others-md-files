# 幫我創一個.cursormcp.json

> Cursor · 2025-11-15 17:56 · `b47ae9c2-f4d4-4a23-98d9-a20a13d62ea1` · 4 則

## 你
幫我創一個.cursor/mcp.json

## Cursor
检查 `.cursor` 目录是否存在，然后创建 `mcp.json`。

## Cursor
创建 `.cursor/mcp.json` 配置文件：

## Cursor
已创建 `.cursor/mcp.json` 配置文件。包含：

- **MCP 服务器配置**：配置了文件系统服务器，允许访问 `be-fastapi` 和 `frontend-vite` 目录

这是一个基础配置。如需添加其他 MCP 服务器或调整设置，请告知。
