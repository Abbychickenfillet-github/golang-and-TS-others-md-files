# how

> Cursor · 2025-09-05 15:28 · `a193adcc-dc48-4bcd-9115-ede89ffd0c89` · 6 則

## 你
?how

## Cursor
I can see you're having an issue with pnpm installation in your Next.js project. The error shows:

```
ERR_PNPM_EISDIR  EISDIR: illegal operation on a directory, symlink 'D:\Users\User\Documents\coding\create-useCallback\my-app\node_modules\.pnpm\scheduler@0.26.0\node_modules\scheduler' -> 'D:\Users\User\Documents\coding\create-useCallback\my-app\node_modules\.pnpm\react-dom@19.1.0_react@19.1.0\node_modules\scheduler'
```

This is a common Windows symlink issue with pnpm. Here are several solutions to try:

## Cursor
Let me fix the PowerShell syntax:

## Cursor
Now let's try a few different approaches to fix the pnpm installation issue:

**Option 1: Try with npm instead of pnpm**

## 你
我剛剛是用pnpm create next-app現在改用npm i 會怎樣嗎

## Cursor
不會有問題的！`pnpm create next-app` 只是用來創建 Next.js 專案，之後用 `npm install` 來安裝依賴是完全沒問題的。

讓我解釋一下：

1. **`pnpm create next-app`** - 只是下載並設置 Next.js 專案的模板文件
2. **`npm install`** - 根據 `package.json` 安裝依賴，不管專案是用什麼工具創建的

兩者都是讀取同一個 `package.json` 文件，所以結果是一樣的。讓我們試試看：
