# 17. 2026-03-20 票券新增頁面、通行碼、UI 改版

## 用戶需求（按時間順序）

### 上午 — 票券進階設定
1. AddTicket/EditTicket Modal 加「進階設定」區塊（限購/轉讓/隱藏票數）
2. 售票時間 responsive（窄螢幕各佔一行）+ 備註移到下方避免高度不對齊
3. `/tickets/create` 獨立新增頁面（雙欄 Card layout）
4. `EventSearchSelect.tsx` 共用活動搜尋元件
5. button nested button 修復（TicketActionButtons 從 AccordionButton 移出）
6. 列表頁加「新增票券（新頁面）」按鈕，用 `variant="lightBrand"` 跟原 Modal 按鈕同色

### 下午 — 通行碼 + 元件複用
7. `access_code` 欄位：Model → DTO → Service → Order 驗證（後端）
8. Dashboard: AddTicket/EditTicket/create/詳情頁 加通行碼 UI
9. Official Website: 購票頁通行碼鎖頭 + 輸入框解鎖
10. **TicketForm.tsx 共用元件**：新增/編輯共用，接收 `ticket` prop
    - `tickets/create.tsx` 用 `<TicketForm />`（新增模式）
    - `tickets/$ticketId_.edit.tsx` 用 `<TicketForm ticket={...} />`（編輯模式）
    - 詳情頁「編輯」按鈕 toggle 展開 inline TicketForm
11. 前置票券列表改 toggle 收合/展開（Collapse）
12. 移除舊 EditTicket Modal

### 傍晚 — Official Website 編輯 + 庫存修復
13. Official Website 票券設定頁 inline 編輯（數量/限購/轉讓/通行碼，價格暫不開放）
14. 票券卡片顯示進階設定狀態（限購/禁止轉讓/需通行碼）
15. **庫存狀態修復**：更新 quantity 後自動修正 sold_out ↔ active
    - 用戶增加 quantity 後剩餘 > 0，但 status 沒從 sold_out 變回 active
    - 後端 `UpdateTicket` 加自動狀態判斷

## 已完成

### 後端
- [x] `tickets.access_code VARCHAR(50)` 欄位 + SQL migration
- [x] Model/DTO/Service 全鏈路映射
- [x] `AddTicketsToOrder` 通行碼驗證（`strings.EqualFold` 大小寫不敏感）
- [x] `UpdateTicket` 自動修正 sold_out/active 狀態

### Dashboard
- [x] `TicketForm.tsx` 共用元件（新增/編輯）
- [x] `EventSearchSelect.tsx` 共用活動搜尋元件
- [x] `/tickets/create` 新增頁面
- [x] `/tickets/$ticketId/edit` 編輯頁面
- [x] 詳情頁 inline 編輯（toggle TicketForm）
- [x] 前置票券 toggle 收合
- [x] 進階設定 Grid 排版
- [x] button nested button 修復
- [x] 通行碼 UI（所有表單 + 詳情頁 Badge）

### Official Website
- [x] 購票頁通行碼鎖頭 + 輸入框 + 解鎖
- [x] 票券設定頁 inline 編輯（數量/限購/轉讓/通行碼）
- [x] 中英文 i18n

### 文件
- [x] 功能規格統一到 `功能規格/` 一個資料夾
- [x] Stack trace 閱讀教學（frontend-historical-errors）
- [x] Test plan: access_code

## 待處理
- [ ] Official Website 票券設定頁版面加寬
- [ ] Official Website 新增/編輯共用元件（TAB 瀏覽 + 票券分組 SubTab）
- [ ] event_relationship 後端 Model → Repository → Service → DTO → Handler
- [ ] event_relationship Dashboard/Official Website UI
- [ ] 後端處理 `process_ecpay` 參數
- [ ] `refunded_count` 歷史資料回填
- [ ] DB migration: `ALTER TABLE tickets ADD COLUMN access_code` (stage + prod)

## 分支狀態
- Dashboard: `feat/ticket-ui-improvements`（從 stage 切出）
- Backend: `hotfix/ticket-transfer-refund-limit`
- Official Website: `hotfix/ticket-transfer-refund-limit`
