# 2026-03-25~26 Dashboard 贈品補發與票券設定

## 分支
- `hotfix/v5-dashboard-coupon-ticket`（backend, dashboard, official_website 三個 repo）

## 今日 TODOs

### 1. [Bug] Dashboard 贈品方案右側購買順序列表顯示錯誤
- **狀態**：✅ 已修（dashboard 前端）

### 2. [Bug] 補發贈品 claim-status API 回傳票券數為 0
- **狀態**：✅ 已修（後端 join order 表）

### 3. [Feature] 票券設定頁加 max_per_order 和 is_transferable 欄位
- **位置**：official_website `EventsCreateTicketSettingsPage.tsx`
- **修法**：formData 加欄位、submit/PATCH 傳值、編輯載入、表單 UI、i18n 翻譯
- **狀態**：✅ 已修（official_website）
- **TODO**：dashboard 的 `ticket-settings.tsx` 也要加（目前只改了 official_website）

### 4. [Bug] order_item.buyer_member_id 未寫入
- **狀態**：⏳ 待修

### 5. [Feature] 補發贈品功能完善
- **狀態**：⏳ 待驗證

### 6. [Bug] 免費票寫死每人限領一次
- **根因**：後端 `hasClaimedFreeTicket` + 前端 `EventRegisterTicketPage` 都寫死 is_free 就擋
- **修法**：
  - 後端：移除 `hasClaimedFreeTicket`，新增 `countMemberTicketClaimed`，依 `max_per_order` 判斷
  - 前端：`handleQuantityChange` 和 UI 標籤都改依 `max_per_order` 判斷
- **狀態**：✅ 已修（後端 + official_website）

### 7. [Bug] ClaimCoupon available_from/to 設了卻被 claim_period 擋住
- **根因**：Phase 3 的 available_from/to 通過了，但 Phase 2 的 checkClaimPeriod 又檢查一次活動時段
- **修法**：有設 available_from/to 時跳過 claim_period 檢查
- **狀態**：✅ 已修（後端）

### 8. [Feature] 贈品方案限定票種 UI（Dashboard）
- **修法**：新增/編輯方案 Modal 加 checkbox 選擇限定票種，方案卡片顯示「限N票種」Badge
- **狀態**：✅ 已修（dashboard）

### 9. [Feature] 免費票券不得退款
- **問題**：免費票券（is_free=true, price=0）不應該允許退款流程
- **狀態**：⏳ 待做

### 10. [Feature] Dashboard ticket-settings 也要加 max_per_member / is_transferable
- **位置**：`futuresign.dashboard/src/routes/_layout/events/create/ticket-settings.tsx`
- **狀態**：⏳ 待做

### 11. [已確認] 品牌商選購電力 — 不同攤位類型的免費電壓
- **結論**：功能正常，前端用 `boothTypeId` → `EventBoothType.base_wattage` 動態顯示
- **狀態**：✅ 無需修改

### 12. [Feature] 退票規則實作與測試
- **退票規則**（已寫在 /refund-policy）：
  - 活動前 7 天以上：全額退款（扣手續費）
  - 活動前 3-7 天：50% 退款
  - 活動前 3 天內：不接受退票
  - 免費票券：可隨時取消，無需退款
- **需實作/驗證**：
  - 後端退款 API 是否依照上述時間規則計算退款比例
  - 免費票退票 = 直接取消持有（不走 ECPay 退款流程）
  - 定金票券退票邏輯
- **狀態**：⏳ 待做

## 備註
- `GetByBuyerMemberID` 在 repository 層被多處引用，不要修改該方法本身
- order.order_type 區分：`b2c_ticket`（消費者購票）/ `b2b_booth`（品牌商租攤位）
- 免費票限購邏輯：`max_per_order = 0` 不限制，`> 0` 才檢查
