# 攤位與地圖管理 UI 整理

> **建立日期**：2026-03-17
> **狀態**：已完成
> **完成後移至**：`工作日誌/`

---

## TODO

### 1. 多地圖篩選跟地圖列表要更靠近
- [x] 將地圖篩選 dropdown 移到 TabList 旁邊，與地圖表格更靠近
- [x] 確認篩選後列表即時更新

### 2. 攤位類型跟區域要靠近
- [x] 攤位類型和區域設定合併到同一個 Card，用分隔線區隔
- [x] 導航列的「攤位類型」和「區域設定」合併為「攤位類型 & 區域」

### 3. 時間價格區間的個別新增/修改 Modal
- [x] 建立 `PricingModal` 元件 (`src/components/Booths/PricingModal.tsx`)
- [x] 支援新增定價（POST `/api/v1/event-booth-types/{id}/pricings`）
- [x] 支援修改定價（PATCH `/api/v1/event-booth-types/pricings/{id}`）
- [x] 支援刪除定價（DELETE `/api/v1/event-booth-types/pricings/{id}`）
- [x] Modal 內容：類型程式碼、顯示名稱、金額、幣別、開始/結束日期、啟用開關
- [x] 驗證：類型程式碼和名稱必填、日期必填
- [x] 新增/修改後即時 refetch + invalidateQueries
- [x] 每個 BoothTypeCard 加上 💲 按鈕開啟 PricingModal

---

## 修改的檔案

| 檔案 | 改了什麼 |
|------|---------|
| `futuresign.dashboard/src/routes/_layout/booths.tsx` | MapSelector 移到 TabList 旁；攤位類型 & 區域合併同 Card；BoothTypeCard 加定價按鈕 |
| `futuresign.dashboard/src/components/Booths/PricingModal.tsx` | 新建：時間定價 CRUD Modal |
